name: Build Flet APK
on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Set up Java
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Clean Old Build Files
      run: |
        rm -rf build/ dist/

    - name: Build APK with Flet
      run: |
        flet build apk . --product "Cable Quotation Generator" --org com.elcocables.quotation --verbose --yes

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: cable-quotation-release-apk
        path: build/apk/*.apk
